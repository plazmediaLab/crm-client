import {
  SELECT_CLIENT,
  SELECT_PRODUCT,
  SELECT_PRODUCTS
} from '../types';

export default ( state, action ) => {
  switch (action.type) {
    // case value:
      
  
    default:
      return state;
  }
};